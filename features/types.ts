const today = new Date();
today.getMinutes();

const person = {
  age: 20
}

class Color {
  name: string;
  
  constructor(name) {
    this.name  = name;
  }
}

const red = new Color('red');
console.log(red)